import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    height: 66,
    width: 62,
    borderRadius: 8,
  },
  guildIconContainer: {
    height: 68,
    width: 64,
    borderRadius: 8,
    alignItems: "center",
    justifyContent: "center",
    marginRight: 20,
  },
});
