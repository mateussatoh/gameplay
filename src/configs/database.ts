const DATABASE_NAME = "@gameplay";

// @nomedoapp:collection

const COLLECTION_USER = `${DATABASE_NAME}:user`;
const COLLECTION_EVENTS = `${DATABASE_NAME}:events`;

export { COLLECTION_EVENTS, COLLECTION_USER };
